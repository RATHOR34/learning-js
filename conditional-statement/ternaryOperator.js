// ternary operator
let age = 17;
let darkTheme = false;

// let message = (age > 18) ? "yes you can give the value" : "no you can't give vote"
// console.log(message)
let output = (age >= 18 && age <= 60) ? "yes you can apply for the licence" : "No you can not apply for the licence"
console.log(output)

let output2 = (darkTheme === true) ? "darkTheme has applyed" : "darkTheme has stopped"
console.log(output2)